{
  "name": "potato",
  "version": "0.0.0",
  "description": "A forum",
  "scripts": {
    "dev": "cd client; webpack -w",
    "pack": "cd client; webpack && gulp",
    "serve": "cd server-go; gin",
    "db": "cd db; docker-compose -p paper-db up --build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "eslint": "cd client; eslint **.js"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=8.9.0"
  }
}
